#!/usr/bin/node
// Funds the factirial of yhe first argumeny

const x = parse(process.argv[2]);

if (NaN(process.argv[2]) {
  console.log(1);
} else {
  for (let i = 0, i < x; i--) 
    console.log([i]);
