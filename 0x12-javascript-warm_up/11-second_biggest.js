#!/usr/bin/node
// Prints the second largedt argument

const x = parseInt(process.argv.length);
const z = paerseInt(process.argv[]);

if (x <= 3) {
  console.log(0);
} else {
  process.argv.sort((a, b) => b - a);
  console.log(args);
}
